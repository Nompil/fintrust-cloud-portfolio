# Week 11 Self Assessment

Checked on 20 September 2026.

| Skill | Current view | Evidence |
| --- | --- | --- |
| Identify the primary WAF pillar in a scenario | Confident when the business outcome is explicit | WAF mapping and six scenario explanations |
| Separate Operational Excellence from Reliability | Improving | Decision rules in the mapping and reflection |
| Explain the cost and sustainability trade-off | Confident | Graviton, Spot and archive decisions in the notes |
| Use `ROW_NUMBER`, `DENSE_RANK` and `LAG` | Practised in code | PostgreSQL query file and local tests |
| Build a three-step CTE chain | Practised in code | `monthly_totals`, `monthly_history` and `growth_alerts` |
| Write a retry decorator with exponential backoff | Practised and tested locally | `pipeline_resilience.py` |
| Use a thread pool for I/O-bound metadata calls | Practised and tested locally | Concurrent S3 metadata helper |
| Run a WAF review or the SQL queries against a supplied account and database | Still requires practical evidence | No AWS or SQL result output was supplied |

The work that needs real follow-up is clear: run the SQL file against the course database, record a benchmark with the learner environment, and save only genuine AWS or LMS evidence.
