# Week 10 Preparation

The next database work should build on the Week 9 migration plan rather than create an unrelated example.

1. Review DMS table validation, large object handling and replication instance sizing.
2. Compare the Oracle objects converted automatically with those needing manual PostgreSQL work.
3. Trace an MGN server from replication through test launch and final cutover.
4. Compare DataSync, S3 Replication and DMS for their different data movement jobs.
5. Define recovery targets before selecting Aurora, DynamoDB or cache replication features.
6. Keep AWS console results separate from locally tested request handling.
