Database Architecture — Week 3

Summary:
- Describe the tables used by `clean_transactions` and related utilities.

Suggested diagram notes:
- `raw_transactions` → transforms → `clean_transactions`
- Keys and sample columns: `transaction_id`, `timestamp`, `amount`, `currency`, `account_id`

How to generate:
- Use draw.io to create an ER diagram and export as PNG into `week03/diagrams/`.
